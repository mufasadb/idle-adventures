# Server
PORT=8080

# Database — single URL takes precedence over individual fields
DB_URL=postgres://postgres:postgres@localhost:5432/idle_rpg?sslmode=disable

# JWT
JWT_SECRET=change-me-in-production
